- Renomeação do repositório `mafhper/github-release-workflows` para `mafhper/release-core`; todos os pins locais (`bootstrap`, dogfood, docs, archetypes, README) atualizados para o novo repositório.
- Título das releases agora inclui a versão (`Release Core vX.Y.Z`); o título base do dogfood passou a `Release Core`.
- Consumidores devem atualizar o pin para `mafhper/release-core/.github/workflows/release.yml@v1.0.6`.