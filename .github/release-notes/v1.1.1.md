- Correção no archetype desktop/Tauri: o passo `tauri-action` agora recebe `GITHUB_TOKEN` via `env` (antes dependia de injeção implícita e falhava com "GITHUB_TOKEN is required" após compilar os instaladores).
- Contrato inalterado nesta linha minor: consumidores podem permanecer em `@v1.1.0` ou atualizar para `@v1.1.1`.