- Correção: caminhos do contrato (`package.json`, lockfiles) agora são resolvidos a partir da raiz do repositório consumidor, mesmo com o `release.config.json` em `.github/`.
- Teste de regressão para o layout `.github/` acrescentado à suíte (co-localização do config na raiz continua coberta).